# Wardgate credentials - copy to .env and fill in values

# Agent keys (for authenticating agents to the gateway)
WARDGATE_AGENT_KEY=your-agent-secret-key

# Admin key (for Web UI and CLI access to manage approvals)
WARDGATE_ADMIN_KEY=your-admin-secret-key

# =============================================================================
# API credentials for presets (use with preset: <name>)
# =============================================================================

# Todoist (https://todoist.com/app/settings/integrations/developer)
WARDGATE_CRED_TODOIST_API_KEY=your-todoist-api-key

# GitHub (https://github.com/settings/tokens)
WARDGATE_CRED_GITHUB_TOKEN=ghp_your-github-token

# Cloudflare (https://dash.cloudflare.com/profile/api-tokens)
WARDGATE_CRED_CLOUDFLARE_TOKEN=your-cloudflare-api-token

# Google Calendar (OAuth2 access token)
WARDGATE_CRED_GOOGLE_CALENDAR=your-google-oauth-token

# Postmark (https://account.postmarkapp.com/servers)
WARDGATE_CRED_POSTMARK_TOKEN=your-postmark-server-token

# Sentry (https://sentry.io/settings/account/api/auth-tokens/)
WARDGATE_CRED_SENTRY_TOKEN=your-sentry-auth-token

# PingPing (https://pingping.io Account -> API)
WARDGATE_CRED_PINGPING_TOKEN=your-pingping-api-key

# Plausible (https://plausible.io/settings)
WARDGATE_CRED_PLAUSIBLE_TOKEN=your-plausible-api-key

# =============================================================================
# IMAP/SMTP credentials (format: username:password)
# =============================================================================

WARDGATE_CRED_IMAP=user@gmail.com:your-app-password
WARDGATE_CRED_SMTP=user@gmail.com:your-app-password

# =============================================================================
# SSH credentials (PEM-encoded private key with escaped newlines)
# =============================================================================

# WARDGATE_SSH_KEY_PROD="-----BEGIN OPENSSH PRIVATE KEY-----\nb3Blbn...\n-----END OPENSSH PRIVATE KEY-----"
