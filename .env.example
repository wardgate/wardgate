# Wardgate credentials - copy to .env and fill in values

# Agent keys (for authenticating agents to the gateway)
WARDGATE_AGENT_TESSA_KEY=your-agent-secret-key

# API credentials (injected into upstream requests)
WARDGATE_CRED_TODOIST_API_KEY=your-todoist-api-key
