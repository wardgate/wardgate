name: smtp
description: "SMTP email sending (generic)"
upstream: smtps://smtp.example.com:465
docs_url: https://github.com/wardgate/wardgate/blob/main/docs/config.md#smtp-endpoints
auth_type: plain
adapter: smtp

capabilities:
  - name: send_email
    description: "Send emails"
    rules:
      - match: { method: POST, path: "/send" }
